{
  "name": "healthcare-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "vercel-build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "db:generate": "npm run db:generate --workspace=apps/api",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "db:seed": "npm run db:seed --workspace=apps/api",
    "db:setup": "npm run db:generate --workspace=apps/api && npm run db:push --workspace=apps/api && npm run db:seed --workspace=apps/api",
    "db:test": "npm run db:generate --workspace=apps/api && node scripts/test-db-connection.mjs",
    "setup:prod": "bash scripts/setup-production.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
